<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rl2"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/rl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/shadow"
        android:descendantFocusability="blocksDescendants"
        android:orientation="vertical">


        <ImageView
            android:id="@+id/backimg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/newsback" />

        <!--<android.support.v7.widget.RecyclerView-->
        <!--android:id="@+id/listView"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent"-->
        <!--android:scrollbars="vertical"/>-->



        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="80dp">



            <Button
                android:id="@+id/listenToAll"
                style="@style/Base.Widget.AppCompat.Button.Borderless.Colored"
                android:layout_width="match_parent"
                android:layout_height="30dp"
                android:background="@drawable/greyback"
                android:text="L  i  s  t  e  n      T  o      A  l  l"
                android:textColor="@android:color/white"
                android:textSize="17dp" />

            <Button
                android:id="@+id/deleteAll"
                style="@style/Base.Widget.AppCompat.Button.Borderless.Colored"
                android:layout_width="match_parent"
                android:layout_height="30dp"
                android:layout_below="@+id/listenToAll"
                android:layout_marginTop="5dp"
                android:background="@drawable/greyback"
                android:text="D  E  L  E  T  E     A  L  L "
                android:textColor="@android:color/white"
                android:textSize="17dp" />

        </RelativeLayout>

        <ListView
            android:id="@+id/listViewTopStories"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="80dp"
            android:background="@drawable/shadow"
            android:divider="@drawable/greyback"
            android:dividerHeight="18dp"
            android:visibility="gone">

        </ListView>

        <ListView
            android:id="@+id/listViewSports"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="80dp"
            android:background="@drawable/shadow"
            android:divider="@drawable/greyback"
            android:dividerHeight="18dp"
            android:visibility="gone">

        </ListView>

        <ListView
            android:id="@+id/listViewTech"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="80dp"
            android:background="@drawable/shadow"
            android:divider="@drawable/greyback"
            android:dividerHeight="18dp"
            android:visibility="gone">

        </ListView>

        <ListView
            android:id="@+id/listViewWorld"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="80dp"
            android:background="@drawable/shadow"
            android:divider="@drawable/greyback"
            android:dividerHeight="18dp"
            android:visibility="gone">

        </ListView>

        <ListView
            android:id="@+id/savedArticle"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="80dp"
            android:background="@drawable/shadow"
            android:divider="@drawable/greyback"
            android:dividerHeight="18dp"
            android:visibility="gone">

        </ListView>

        <!---->
        <!--<ProgressBar-->
        <!--android:id="@+id/progressBar"-->
        <!--style="?android:attr/progressBarStyleLarge"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_centerInParent="true" />-->

    </RelativeLayout>
</RelativeLayout>

